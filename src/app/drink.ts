export interface Drink {
  id: number; // string or number?
  name: string;
  ingredients: string;
  quantity: number; // string or number
  img: string;
  instructions: string;
  mainColor: string;
  secondaryColor: string;
}
